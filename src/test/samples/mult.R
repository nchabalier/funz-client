mult <- function(x) {
	x[1]*x[2]
}

t=0

Sys.sleep(t);
cat('1/3');
Sys.sleep(t);
cat('2/3');
Sys.sleep(t);
cat('3/3');
Sys.sleep(t);

#!: f <- function(x) {x}

cat('X = ',c( ?x1 , ?[x2~.5]),'\n');

if ( ?x1 <= 0 ) {
  cat('z = ',mult(c( ?x1 , !{f(?x2)} )),'\n')
} else {
  cat('z = ?','\n')
}
